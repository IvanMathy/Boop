/**
  {
    "api":1,
    "name":"Decode Unicode String ",
    "description":"Returns a readable string from the unicode encoded string (js format)",
    "author":"gokhanakgul",
    "icon":"broom",
    "tags":"string,normalize,convert,readable,unicode"
  }
**/


function main(state) {
  state.text = unicodeToChar(state.text);
}

function unicodeToChar(text) {
   return text.replace(/\\u[\dA-F]{4}/gi, 
          function (match) {
               return String.fromCharCode(parseInt(match.replace(/\\u/g, ''), 16));
          });
}
